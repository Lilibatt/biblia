@model IEnumerable<biblia_курсач.Models.issuance_of_books>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.date_of_issue)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.return_date)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.books.name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.librarian.FIO)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.reader.FIO)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.date_of_issue)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.return_date)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.books.name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.librarian.FIO)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.reader.FIO)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id }) |
            @Html.ActionLink("Details", "Details", new { id=item.id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id })
        </td>
    </tr>
}

</table>
